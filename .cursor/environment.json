{
  "name": "ApplyWizz Signal",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "web",
      "command": "pnpm dev",
      "description": "Next.js dev server for apps/web on http://localhost:3000 (reads apps/web/.env.local)."
    }
  ],
  "ports": [{ "name": "web", "port": 3000 }]
}
