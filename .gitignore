# dependencies
node_modules/
.pnpm-store/

# build output
.next/
out/
build/
dist/

# testing
coverage/

# env files (never commit real secrets)
.env
.env.*
!.env.example

# misc
.DS_Store
*.tsbuildinfo
.vercel

# supabase local runtime state
supabase/.temp/
supabase/.branches/

# infrastructure secrets (SSH keys, generated credentials) — never commit
.azure-secrets/

# M8 language-evaluation audio + benchmark evidence — local artifacts only
# (real/licensed third-party audio, real human recordings). Preserved on
# disk, never committed into the product repo.
m8-audio/
